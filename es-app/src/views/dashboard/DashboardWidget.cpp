// Clon3D Cyberdeck OS — Dashboard Widget Base
#include "views/dashboard/DashboardWidget.h"

DashboardWidget::DashboardWidget(Window* window)
    : GuiComponent(window), mAnchor(Anchor::HUD_CENTER)
{
}
